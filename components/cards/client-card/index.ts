// Este archivo sirve como punto de entrada para el componente ClientCard
// Exporta tanto el componente como sus tipos para facilitar su importación

// Exportamos el componente ClientCard como exportación nombrada
export { ClientCard } from "./ClientCard"

// Exportamos el tipo ClientCardProps para que pueda ser usado en otros archivos
export type { ClientCardProps } from "./ClientCard"

// También exportamos ClientCard como exportación por defecto para mantener compatibilidad
export { ClientCard as default } from "./ClientCard"
